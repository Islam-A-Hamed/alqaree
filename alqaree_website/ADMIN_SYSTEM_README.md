# نظام الإدارة - دليل المطور

## نظرة عامة
نظام إدارة شامل لموقع القارئ الإسلامي يتيح إدارة جميع المحتوى.

## المميزات الأساسية

### 👨‍💼 إدارة المستخدمين
- تسجيل دخول آمن
- إدارة الصلاحيات
- تتبع الأنشطة

### 📝 إدارة المقالات
- إضافة/تعديل/حذف المقالات
- تصنيف المقالات
- إدارة حالة النشر

### 🎵 إدارة التلاوات
- رفع وإدارة التلاوات
- ربط بالقراء والسور
- إدارة روابط YouTube

### 🎤 إدارة المواعظ
- إدارة المواعظ والدروس
- ربط بالمتحدثين
- إدارة المحتوى الصوتي

## ملفات النظام

### الملفات الأساسية
- `admin/index.php` - لوحة التحكم الرئيسية
- `admin/login.php` - صفحة تسجيل الدخول
- `admin/logout.php` - تسجيل الخروج

### ملفات الإدارة
- `admin/manage-articles.php` - إدارة المقالات
- `admin/manage-tilawat.php` - إدارة التلاوات
- `admin/manage-hekum.php` - إدارة المواعظ

### ملفات المساعدة
- `admin/create_admin.php` - إنشاء مدير جديد
- `admin/setup_admin.php` - إعداد النظام
- `admin/init_database.php` - تهيئة قاعدة البيانات

## الأمان

### حماية البيانات
- تشفير كلمات المرور
- حماية من SQL Injection
- التحقق من صحة البيانات

### إدارة الجلسات
- جلسات آمنة
- انتهاء تلقائي للجلسات
- تتبع نشاط المستخدمين

## قاعدة البيانات

### الجداول الأساسية
- `admin_accounts` - حسابات المديرين
- `articles` - المقالات
- `tilawat` - التلاوات
- `hekum` - المواعظ
- `activity_logs` - سجل الأنشطة

### العلاقات
- كل جدول مرتبط بحساب المدير الذي أنشأه
- تتبع آخر تحديث لكل سجل
- حفظ تاريخ الإنشاء والتعديل

## API Endpoints

### مقالات
- `get_random_article.php` - مقال عشوائي
- `check_articles.php` - فحص المقالات

### قرآن
- `get_random_ayah.php` - آية عشوائية

## التطوير

### متطلبات النظام
- PHP 7.4+
- MySQL 5.7+
- Apache/Nginx

### المكتبات المستخدمة
- PDO لقاعدة البيانات
- Sessions لإدارة الجلسات
- File Upload لرفع الملفات

## استكشاف الأخطاء

### مشاكل شائعة
1. **خطأ في تسجيل الدخول**
   - تحقق من صحة البيانات في `admin_accounts`

2. **مشكلة في رفع الملفات**
   - تحقق من صلاحيات المجلدات
   - فحص حجم الملفات المسموح

3. **خطأ في قاعدة البيانات**
   - فحص اتصال قاعدة البيانات
   - التحقق من هيكل الجداول

## الصيانة

### النسخ الاحتياطي
- نسخ احتياطي يومي لقاعدة البيانات
- حفظ الملفات المرفوعة
- تتبع التغييرات

### التحديثات
- تحديث منتظم للأمان
- اختبار التحديثات في بيئة التطوير
- حفظ سجل التحديثات

## الدعم

للمساعدة أو الاستفسارات، يرجى مراجعة:
- ملفات السجل في `logs/`
- رسائل الخطأ في `php_errors.log`
- توثيق API في كود الملفات
