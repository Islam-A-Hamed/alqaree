# تحديثات لوحة الإدارة

## الميزات الجديدة المضافة

### 1. إعدادات المستخدم
تم إضافة قسم جديد في الصفحة الرئيسية (`index.php`) يتيح للمستخدم:

- **تغيير اسم المستخدم**: إمكانية تغيير اسم الدخول
- **تغيير كلمة المرور**: إمكانية تحديث كلمة المرور مع التحقق من كلمة المرور الحالية
- **زر إظهار/إخفاء كلمة المرور**: لجميع حقول كلمة المرور لتسهيل الكتابة

### 1.1 الترحيب الشخصي
- **جمل ترحيب مخصصة:** تحتوي على اسم المستخدم
- **عناوين صفحات ديناميكية:** تتغير حسب المستخدم الحالي

### 2. تحسين نظام تسجيل الدخول
تم تحديث نظام تسجيل الدخول (`login.php`) ليستخدم قاعدة البيانات بدلاً من القيم المباشرة:

- التحقق من صحة كلمة المرور باستخدام `password_verify()`
- تحديث وقت آخر دخول للمستخدم
- رسائل خطأ أكثر تفصيلاً

## كيفية الاستخدام

### لإدارة إعدادات المستخدم:

1. اذهب إلى الصفحة الرئيسية للوحة الإدارة
2. في أسفل الصفحة ستجد قسم "إعدادات المستخدم"
3. يمكنك:
   - تغيير اسم المستخدم
   - تغيير كلمة المرور (اختياري)
   - استخدام أزرار 👁️ لإظهار/إخفاء كلمة المرور

### ملاحظات مهمة:

- **كلمة المرور الحالية مطلوبة** لأي تغيير
- **كلمة المرور الجديدة** يجب أن تكون 6 أحرف على الأقل
- **تأكيد كلمة المرور** مطلوب عند تغيير كلمة المرور
- البيانات محفوظة بأمان في قاعدة البيانات مع تشفير bcrypt

## البيانات الافتراضية

- **اسم المستخدم**: admin
- **كلمة المرور**: alqaree2024

## إعدادات المستخدم

### الوصول للإعدادات:
- **من لوحة التحكم الرئيسية:** اضغط على "⚙️ إعدادات المستخدم" في الشريط الجانبي
- **الرابط المباشر:** `admin/user_settings.php`

### تصميم الشريط الجانبي:
- **الترتيب من الأعلى للأسفل:**
  1. شعار الموقع (sidebar-header)
  2. معلومات المستخدم (sidebar-user-info) ← **جديد**
  3. قائمة التنقل (sidebar-nav)
  4. زر تسجيل الخروج (sidebar-footer)

### الروابط والأيقونات:
- **📊 لوحة التحكم:** `index.php`
- **🎵 إدارة التلاوات:** `manage-tilawat.php`
- **📖 إدارة المواعظ:** `manage-hekum.php`
- **⚙️ إعدادات المستخدم:** `user_settings.php`
- **🚪 تسجيل الخروج:** `logout.php`

### مميزات الروابط:
- **تأثيرات hover** مع انتقال سلس
- **الرابط النشط** يظهر بلون مختلف
- **تصميم متجاوب** للأجهزة المحمولة
- **أيقونات واضحة** ومتناسقة

### الميزات المتاحة:
- **تغيير اسم المستخدم:** مع التحقق من عدم التكرار
- **تغيير كلمة المرور:** مع تأكيد الكلمة الجديدة
- **عرض معلومات الحساب:** اسم المستخدم، الدور، وقت آخر دخول
- **إظهار/إخفاء كلمة المرور:** أزرار 👁️ بجانب حقول كلمة المرور

### الترحيب الشخصي:
- **لوحة التحكم:** "مرحباً [اسم المستخدم] في لوحة الإدارة"
- **إعدادات المستخدم:** "إعدادات [اسم المستخدم]"
- **عنوان الصفحة:** يتضمن اسم المستخدم

### الأمان:
- كلمات المرور مشفرة باستخدام `password_hash()` و `PASSWORD_DEFAULT`
- التحقق من صحة البيانات على الجانب الخادم
- حماية من SQL Injection باستخدام prepared statements
- التحقق من صحة الجلسة قبل السماح بالتغييرات

## استكشاف الأخطاء

### إذا ظهر خطأ "Undefined array key 'admin_role'":
1. قم بتسجيل الخروج وإعادة تسجيل الدخول
2. أو قم بزيارة `admin/test_db.php` للتأكد من وجود بيانات المستخدم
3. النظام سيحاول إصلاح المشكلة تلقائياً

### إذا ظهر خطأ "mysqli object is already closed":
1. هذا خطأ تم إصلاحه - تأكد من أن الملف محدث
2. إذا استمر الخطأ، قم بإعادة تحميل الصفحة
3. المشكلة كانت في إغلاق اتصال قاعدة البيانات مبكراً

### إذا ظهر خطأ "اسم المستخدم غير موجود":
#### الحل السريع:
1. في صفحة تسجيل الدخول، اضغط على رابط **"🔧 إصلاح تسجيل الدخول"** الأحمر
2. أو اذهب مباشرة إلى `admin/fix_login.php`

#### الحلول الأخرى:
1. تشغيل ملف `sql/database.sql` في phpMyAdmin
2. زيارة `admin/create_admin.php` لإنشاء المستخدم الافتراضي
3. زيارة `admin/test_db.php` للتحقق من حالة قاعدة البيانات

#### البيانات الافتراضية:
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `alqaree2024`

### إذا ظهر خطأ "كلمة المرور غير صحيحة":
#### الحل السريع:
1. في صفحة تسجيل الدخول، اضغط على رابط **"🔑 إعادة تعيين كلمة المرور"** الأحمر
2. أو اذهب مباشرة إلى `admin/reset_password.php`

#### الحلول الأخرى:
1. تأكد من أن كلمة المرور هي `alqaree2024` (حساسة لحالة الأحرف)
2. قم بإعادة تعيين كلمة المرور من خلال قاعدة البيانات
3. تحقق من hash كلمة المرور في جدول admin_accounts

### إذا لم يعمل تسجيل الدخول:
1. تأكد من تشغيل XAMPP (Apache و MySQL)
2. تأكد من وجود قاعدة البيانات `alqaree_website`
3. تأكد من وجود الجدول `admin_accounts` والبيانات فيه
4. يمكن تشغيل ملف `sql/database.sql` لإنشاء قاعدة البيانات
5. أو تشغيل `admin/setup_admin.php` لإنشاء المستخدم الافتراضي

### إذا لم تظهر إعدادات المستخدم:
1. تأكد من تسجيل الدخول بنجاح
2. تأكد من وجود `admin_id` في الجلسة
3. تحقق من وجود أخطاء في console المتصفح
4. قم بزيارة `admin/test_db.php` للتحقق من قاعدة البيانات

### اختبار النظام:
- `admin/test_db.php` - اختبار قاعدة البيانات والمستخدمين
- `admin/setup_admin.php` - إنشاء المستخدم الافتراضي إذا لم يكن موجوداً

## التحديثات المستقبلية المقترحة

- إضافة صورة الملف الشخصي
- إضافة البريد الإلكتروني
- إضافة سجل العمليات
- إضافة إشعارات
